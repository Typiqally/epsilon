version: "3.8"

services:
  api:
    image: typiqally/epsilon:docker-api
    ports:
      - "5003:443"
    volumes:
      - type: bind
        source: ./appsettings.json
        target: /app/appsettings.json
  frontend:
    image: typiqally/epsilon:docker-frontend
    ports:
      - "5004:443"